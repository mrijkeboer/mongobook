%h1 Add website
= form_for(:website, :url => contact_websites_url(@contact.id)) do |form|
  %table.edit
    %tr
      %th
        %label{:for => "website_url"} Website:
      %td= form.text_field :url
      %td= @website.errors[:url].first
    %tr
      %th &nbsp;
      %td= submit_tag "Add", :class => "button"
