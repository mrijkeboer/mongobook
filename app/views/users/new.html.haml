%h1 New user
= form_for(:user, :url => users_url) do |f|
  %table.edit
    %tr
      %th= f.label :username, "Username:"
      %td= f.text_field :username
      %td.error= @user.errors[:username].first
    %tr
      %th= f.label :password, "Password:"
      %td= f.password_field :password
      %td.error= @user.errors[:password].first
    %tr
      %th= f.label :password_confirmation, "Confirm:"
      %td= f.password_field :password_confirmation
      %td.error= @user.errors[:password_confirmation].first
    %tr
      %th &nbsp;
      %td= f.submit "Save", :class => "button"
